# Two application server deployment

This production-only runbook has been superseded because the same two application servers now host isolated staging and production stacks.

Use [Staging and production deployment on two application servers](staging-production-deployment.md). It includes the current command signatures, server-generated repository keys, Base64-validated GitHub Actions key, environment isolation, approval gates, and rolling load-balancer drain process.
